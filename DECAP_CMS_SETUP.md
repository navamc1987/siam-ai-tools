# Decap CMS - คู่มือการตั้งค่าและการใช้งาน

## ✅ การติดตั้งเสร็จสิ้น

ผมได้ติดตั้ง **Decap CMS** (GitHub-based CMS) สำหรับจัดการรูปภาพและผลงานของเว็บไซต์ของคุณเรียบร้อยแล้ว

## 📁 ไฟล์ที่สร้างขึ้น

```
public/admin/
├── index.html          # หน้าเข้าสู่ระบบ CMS
└── config.yml          # ตั้งค่า Collections และ Fields

portfolio/              # โฟลเดอร์เก็บไฟล์ผลงาน (Markdown)

client/src/pages/
└── Portfolio.tsx       # หน้าแสดงผลงาน

client/src/components/
└── Navbar.tsx          # (อัปเดต) เพิ่มลิงก์ "ผลงานของเรา"
```

## 🚀 วิธีการเข้าใช้งาน Decap CMS

### ขั้นตอนที่ 1: ตั้งค่า GitHub OAuth (ครั้งแรกเท่านั้น)

1. ไปที่ **GitHub Settings** → **Developer settings** → **OAuth Apps**
2. กดปุ่ม **New OAuth App** แล้วกรอกข้อมูล:
   - **Application name**: `Siam AI Tools CMS`
   - **Homepage URL**: `https://siamai.cloud`
   - **Authorization callback URL**: `https://siamai.cloud/admin/oauth`
3. คัดลอก **Client ID** และ **Client Secret** ไว้
4. ตั้งค่า Environment Variables บนเซิร์ฟเวอร์:
   ```
   DECAP_CMS_CLIENT_ID=<Client ID>
   DECAP_CMS_CLIENT_SECRET=<Client Secret>
   ```

### ขั้นตอนที่ 2: เข้าสู่ CMS

1. เปิด `https://siamai.cloud/admin` (หรือ `http://localhost:3000/admin` สำหรับการทดสอบ)
2. กดปุ่ม **Login with GitHub**
3. ยืนยันการเข้าถึง GitHub
4. ตอนนี้คุณสามารถเพิ่มผลงานได้แล้ว!

## 📝 การเพิ่มผลงานใหม่

1. ในหน้า CMS ให้ไปที่ **Collections** → **ผลงานของเรา**
2. กดปุ่ม **New Portfolio** แล้วกรอกข้อมูล:
   - **ชื่อโครงการ**: เช่น "รีโนเวทห้องน้ำ บ้านพัก"
   - **หมวดหมู่**: เลือกจาก dropdown
   - **รายละเอียดโครงการ**: คำบรรยายเกี่ยวกับงาน
   - **สถานที่**: เช่น "ชลบุรี"
   - **วันที่เสร็จสิ้น**: วันที่เสร็จโครงการ
   - **รูปภาพหลัก**: อัปโหลดรูปภาพหลักของโครงการ
   - **แกลเลอรี่รูปภาพ**: เพิ่มรูปภาพเพิ่มเติม (ได้หลายรูป)
   - **ลิงก์เพิ่มเติม**: (ตัวเลือก) ลิงก์ไปยังเว็บไซต์อื่นหรือรายละเอียด
   - **เผยแพร่**: เลือก "Yes" เพื่อให้ปรากฏบนเว็บไซต์

3. กดปุ่ม **Publish** เพื่อบันทึก

## 🖼️ ข้อมูลรูปภาพ

- **รูปภาพหลัก (Featured Image)**: ขนาดแนะนำ 1200x800px (อัตราส่วน 3:2)
- **รูปภาพในแกลเลอรี่**: ขนาดแนะนำ 1200x900px ขึ้นไป
- **ขนาดไฟล์**: ไม่เกิน 5MB ต่อไฟล์
- **รูปแบบ**: JPG, PNG, WebP

## 📊 หมวดหมู่ผลงาน

ปัจจุบันมีหมวดหมู่ดังนี้:
- ต่อเติมและรีโนเวท
- ติดตั้งโซล่าเซลล์
- ระบบไฟฟ้า
- ระบบแสงสว่าง
- ระบบ CCTV
- อื่น ๆ

(สามารถเพิ่มหมวดหมู่ได้โดยแก้ไข `config.yml`)

## 🔄 การอัปเดตเว็บไซต์

หลังจากที่คุณ **Publish** ผลงานใหม่ใน CMS:

1. ไฟล์ Markdown จะถูกบันทึกในโฟลเดอร์ `portfolio/` ของ GitHub
2. คุณต้อง **Pull** ไฟล์ใหม่บนเซิร์ฟเวอร์:
   ```bash
   cd /path/to/siam-ai-tools
   git pull origin main
   pnpm build
   ```
3. หรือหากตั้งค่า Auto-deploy ให้ GitHub Actions ทำให้อัตโนมัติ

## 🎨 การแก้ไขหน้า Portfolio

หากต้องการปรับแต่งการแสดงผลหน้า Portfolio:

- ไฟล์: `client/src/pages/Portfolio.tsx`
- ปรับแต่ง:
  - สีและ layout
  - จำนวนคอลัมน์ (Grid)
  - ข้อมูลที่แสดง (เพิ่ม/ลบ fields)

## 📌 หมายเหตุสำคัญ

1. **GitHub Authentication**: ต้องตั้งค่า OAuth ให้ถูกต้องเพื่อให้ CMS ทำงาน
2. **ไฟล์ Markdown**: ไฟล์ที่สร้างจาก CMS จะถูกเก็บเป็น Markdown ในโฟลเดอร์ `portfolio/`
3. **รูปภาพ**: รูปภาพจะถูกเก็บในโฟลเดอร์ `public/portfolio-images/`
4. **ข้อมูลตัวอย่าง**: หน้า Portfolio มีข้อมูลตัวอย่าง 3 รายการ สำหรับการทดสอบ

## 🆘 ปัญหาที่อาจเกิดขึ้น

### ปัญหา: ไม่สามารถเข้าสู่ระบบ CMS
**วิธีแก้**: ตรวจสอบ:
- URL ถูกต้อง (`https://siamai.cloud/admin`)
- OAuth App ถูกตั้งค่าใน GitHub
- `config.yml` มี `base_url` ที่ถูกต้อง

### ปัญหา: รูปภาพไม่แสดงบนเว็บไซต์
**วิธีแก้**:
- ตรวจสอบว่า `public_folder` ใน `config.yml` ถูกต้อง
- ตรวจสอบว่ารูปภาพถูกอัปโหลดไปยัง GitHub
- รัน `git pull` และ `pnpm build` ใหม่

### ปัญหา: ผลงานไม่ปรากฏบนเว็บไซต์
**วิธีแก้**:
- ตรวจสอบว่า `published` ถูกตั้งเป็น `true`
- รัน `git pull` และ `pnpm build` ใหม่
- ล้าง Cache ของเบราว์เซอร์

## 📞 ติดต่อสำหรับความช่วยเหลือ

หากมีปัญหาหรือต้องการปรับแต่งเพิ่มเติม โปรดติดต่อทีมพัฒนา

---

**สร้างเมื่อ**: May 16, 2026
**เวอร์ชัน**: 1.0
